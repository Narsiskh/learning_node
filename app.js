function sayHello(name) {
    console.log('Hello ' + name);
}

// sayHello('Narcis')
console.log(window); // Error: window is not defined // In Node we don`t have the window or document objects
// In Node we have other objects to work with files with the operating system with the network and so on.